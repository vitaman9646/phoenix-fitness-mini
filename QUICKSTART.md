# 🚀 Быстрый старт

## За 5 минут до запуска

### 1️⃣ Настрой Telegram бота

```
1. Открой @BotFather в Telegram
2. Отправь /newbot
3. Придумай имя (например: Fitness Coach Vitaliy)
4. Придумай username (например: FitnessCoachVitaliyBot)
5. Скопируй токен (выглядит так: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz)
```

### 2️⃣ Узнай свой Chat ID

```
1. Открой @userinfobot
2. Отправь ему любое сообщение
3. Скопируй свой ID (число, например: 123456789)
```

### 3️⃣ Вставь данные в код

Открой `js/app.js` и в начале файла замени:

```javascript
const CONFIG = {
  BOT_TOKEN: '123456789:ABCdefGHIjklMNOpqrsTUVwxyz', // ← Вставь свой токен
  CHAT_ID: '123456789',                              // ← Вставь свой chat ID
  ANALYTICS_ENABLED: true
};
```

### 4️⃣ Добавь placeholder изображения

В `index.html` замени все пути к изображениям:

**Было:**
```html
<img data-src="images/case1_before.jpg" alt="До">
```

**Стало:**
```html
<img data-src="https://via.placeholder.com/300x400/0a0e17/00e5ff?text=Before" alt="До">
```

### 5️⃣ Запусти локально

```bash
# Вариант 1: Python
python -m http.server 3000

# Вариант 2: Node.js
npx serve

# Вариант 3: Live Server (VS Code расширение)
Просто открой index.html и нажми "Go Live"
```

### 6️⃣ Создай туннель с ngrok

```bash
# Скачай ngrok: https://ngrok.com/download
ngrok http 3000
```

Скопируй HTTPS URL (например: `https://abc123.ngrok.io`)

### 7️⃣ Подключи к Telegram

```
1. Открой @BotFather
2. Выбери своего бота
3. Нажми "Bot Settings" → "Menu Button" → "Edit Menu Button URL"
4. Вставь ngrok URL
5. Или: "Edit Commands" → добавь /start с описанием
```

### 8️⃣ Тестируй!

```
1. Открой своего бота в Telegram
2. Нажми на кнопку Menu (внизу)
3. Приложение откроется!
```

---

## 🎨 Без изображений

Если хочешь запустить вообще без картинок:

1. Открой `index.html`
2. Найди все блоки `.case-slide`
3. Удали блок `.case-photos` полностью

Или просто закомментируй:
```html
<!-- <div class="case-photos">...</div> -->
```

---

## ✅ Чек-лист готовности

- [ ] BOT_TOKEN вставлен
- [ ] CHAT_ID вставлен
- [ ] Изображения заменены на placeholder'ы (или удалены)
- [ ] Локальный сервер запущен
- [ ] ngrok запущен и HTTPS URL скопирован
- [ ] URL добавлен в @BotFather
- [ ] Бот открывается и работает

---

## 🐛 Если что-то не работает

### Приложение не открывается
- Проверь, что ngrok запущен
- Убедись, что используешь HTTPS URL (не HTTP!)
- Telegram требует HTTPS для Web Apps

### Форма не отправляется
- Открой консоль браузера (F12)
- Проверь, есть ли ошибки
- Убедись, что BOT_TOKEN и CHAT_ID правильные

### Изображения не загружаются
- Замени все пути на placeholder URL
- Или удали блоки с фотографиями

### Квиз не работает
- Проверь консоль на ошибки
- Убедись, что все файлы подключены

---

## 📱 Production деплой

Когда всё работает локально, задеплой на:

1. **GitHub Pages** (бесплатно)
2. **Vercel** (бесплатно)
3. **Netlify** (бесплатно)

Подробнее в README.md

---

Готово! Теперь у тебя полноценное Telegram Mini App 🎉
